let product ={
    productName : "Laptop",
    price : 30000,
    color : "green",
    weight : "60g",
    ram : "16gb",
    memory : "512gb",
}

let keys = Object.keys(product);
let values = Object.values(product);
let entries = Object.entries(product);

console.log(keys);
console.log(values);
console.log(entries);